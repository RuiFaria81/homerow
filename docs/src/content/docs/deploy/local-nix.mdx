---
title: Deploy with Local Nix
description: Nix local deploy entrypoint (moved to unified local page).
sidebar:
  hidden: true
---

This page moved to [Deploy Locally](/deploy/local/).

Use the **Nix / NixOS** section there:

```bash
./hrow deploy --via local
```
